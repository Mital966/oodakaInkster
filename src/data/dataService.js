// Public data-access API used by the UI.
//
// This module is now a thin compatibility layer over src/services, which
// chooses between the Supabase backend and the local demo backend at runtime.
// The function signatures used by the pages are unchanged, so components never
// depends on where the data comes from.

export {
  getTattoos,
  getTattooById,
  getFeaturedTattoos,
  getTattoosByArtist,
  getTattoosByCategory,
  getRelatedTattoos,
  getArtists,
  getArtistById,
  getCategories,
  getReviews,
  getOffers,
  getSettings,
  getEnquiries,
  createEnquiry,
  createTattoo,
  updateTattoo,
  deleteTattoo,
  toggleTattooPublish,
  toggleTattooFeatured,
  saveTattooMedia,
  createArtist,
  updateArtist,
  deleteArtist,
  uploadArtistPortrait,
  createCategory,
  updateCategory,
  deleteCategory,
  reorderCategory,
  createReview,
  updateReview,
  deleteReview,
  uploadReviewPhoto,
  createOffer,
  updateOffer,
  deleteOffer,
  getAllOffers,
  saveSettings,
  uploadWebsiteImage,
  backendMode,
  backendLabel,
  remoteConfigured,
  MAX_IMAGE_BYTES,
  MAX_VIDEO_BYTES,
} from '../services'